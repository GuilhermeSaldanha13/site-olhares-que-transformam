# TODO

- [x] Identificar fluxo atual: formulário em `components/landing/inscricao.tsx` chama `/api/submit`.
- [ ] Alterar `components/landing/inscricao.tsx` para enviar via WhatsApp (abrir `wa.me` com mensagem pré-preenchida) em vez de chamar `/api/submit`.
- [x] Definir número de WhatsApp do destinatário (ex.: `+55...`) e template de mensagem com os campos do formulário.
- [ ] Validar que o botão “Enviar inscrição” funciona e exibe feedback após abrir o WhatsApp.
